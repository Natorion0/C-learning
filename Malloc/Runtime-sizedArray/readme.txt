This program will ask the user how many integers to store 
allocate an array of n ints using malloc
check that the allocation has succeeded 
Reads in n integers from the user, storing them via pointer arithmetic
computes the sum and average of n values and prints them and frees the allocated memory before exiting. 
